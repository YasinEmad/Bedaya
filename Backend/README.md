# Bedaya Backend

Minimal Node.js + Express backend for the Bedaya project.

Quick start:

```bash
cd Backend
npm install
npm start
```

Endpoints:
- `GET /` — returns status JSON
- `GET /health` — returns OK
- `POST /echo` — echoes JSON body
